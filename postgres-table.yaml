- name: create postgres tabble
  hosts: all
  tasks:
    - name: Hello Message
      debug:
        msg: "hi pradeep"
    - name: Instalation of postgresql-9.6
      apt:
        name: postgresql-9.6

     - name: start postgresql service
       service: name=postgresql state=restarted enabled=yes
      
    - name: create a database
      postgresql_table:
        db: postgres
        table: joshidb
    #  become_user: postgres
       become: yes
       become_method: sudo
       become_user: root



    

